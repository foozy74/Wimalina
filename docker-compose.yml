services:
  wimalina-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wimalina_uk_container
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
